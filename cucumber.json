{
  "default": {
    "paths": ["features/**/*.feature"],
    "require": ["src/steps/**/*.ts", "src/hooks/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": [
      "summary",
      "progress-bar",
      "html:cucumber-report.html",
      "junit:./allure-results/junit-report.xml"
    ],
    "publishQuiet": true
  }
}